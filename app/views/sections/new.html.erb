<%= render 'shared/modal', title: "New Section" do %>
  <%= form_with(model: @section, url: to_do_list_sections_path(@to_do_list), class: "mb-7") do |form| %>
    <%= render "form", form: %>
  <% end %>
<% end %>  
